# Infralog Documentation

This directory contains the Docusaurus documentation site for Infralog.

## Development

```bash
cd docs
npm install
npm start
```

This starts a local development server at http://localhost:3000.

## Build

```bash
npm run build
```

Generates static content in the `build` directory.
